#include <iostream>
#include <cmath>
using namespace std;
double number_1(double y)
{
    double t;
    double x;
    y=(sin(pow(x,2))/cos(pow(x,2))+sqrt(x,2))/(t*std::log(x+s));
    return y;
}
double number_2(double y)
{
    double t;
    double x;
    y=(1+sqrt(s*x,2))/(sqrt(1+pow(x,3)));
    return y;
}
int main()
{
    cout << << endl;
    return 0;
}
